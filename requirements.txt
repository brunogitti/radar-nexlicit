requests==2.34.2
PyYAML==6.0.3
pytest==9.1.1
python-dotenv==1.2.2
streamlit==1.59.2
# Fixa o starlette (dependencia transitiva do streamlit, usada por baixo do
# painel) numa versao anterior a 1.4.0. A partir da 1.4.0, GZipResponder
# passou a exigir um parametro novo (thread_minimum_size) que o streamlit
# 1.59.2 nao passa nessa chamada, quebrando o painel com TypeError assim que
# um ambiente novo resolve o starlette mais recente sem essa trava. Ainda
# nao corrigido nem na ultima versao do streamlit disponivel (1.61.0,
# checado em 03/08/2026). Remover essa trava quando o streamlit lancar uma
# versao que passe thread_minimum_size na chamada.
starlette==1.3.1
